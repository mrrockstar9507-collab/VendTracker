# VendTracker (PWA) - Updated

Changes in this build:
- Machine list is vertically scrollable on small screens.
- "Add / Update Machine" stays above the Machine List on mobile.
- New: Inventory per Machine (on-hand + optional par) inside the Machines tab.

Data notes:
- Data is stored locally in the browser on each device.
- Use Export/Import JSON to move data between devices.
